V15 WhatsApp Final Fix

- Removed hardcoded old WhatsApp launcher from inner pages.
- One shared WhatsApp component is now injected from shared.js on every page, including homepage.
- Round animated launcher is forced by shared.js and final CSS override.
- Homepage launcher click opens the live-chat popup and does not redirect to Contact page.
- Inner pages now show the same round button, not the old pill/button.
- No WhatsApp number is visible.

After deployment, hard refresh: Ctrl + Shift + R.
