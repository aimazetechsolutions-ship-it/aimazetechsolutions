AimAze Enterprise Website v6 fixes:

1. Removed the top gradient announcement/topbar from every HTML page.
2. Disabled/removes any dynamic topbar injection in shared.js.
3. Removed homepage fallback background image behind the video.
4. Updated homepage video sizing to cover the full hero area.
5. Updated all inner page hero videos to fill the full hero space with no blank/black gaps.
6. Added CSS override rules at the end of site.css to enforce these changes across all pages.

Note: YouTube videos may still contain text or visual elements inside the actual video itself. That is not a background image from the website code. To fully control the visuals, replace the YouTube ID in content.json with a clean corporate video or use a self-hosted MP4.
